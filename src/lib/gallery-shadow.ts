/** Solid 8px frame — pure white; box-shadow avoids shrinking the image box. */
export const GALLERY_IMAGE_BORDER = '0 0 0 8px #fff';

/** Tailwind utility — always-on frame (e.g. mobile portfolio center, lightbox). */
export const GALLERY_IMAGE_BORDER_CLASS = 'shadow-[0_0_0_8px_#fff]';

/** Tailwind utility — desktop portfolio hover frame. */
export const GALLERY_IMAGE_BORDER_HOVER_CLASS = 'md:hover:shadow-[0_0_0_8px_#fff]';
